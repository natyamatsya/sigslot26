# UndefinedBehaviorSanitizer suppressions for sigslot26 tests
#
# These suppress known false positives in Qt and glib internals.

# Qt internal alignment issues
alignment:libQt6Core.so

# glib type punning
type-punning:libglib-2.0.so

# Known Qt integer overflow (false positives)
integer-overflow:libQt6Core.so

# stdexec internal UB (if any)
undefined:libstdc++.so
